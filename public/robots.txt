User-agent: *
Allow: /

# The management area is behind authentication and carries `noindex` on every
# page. Disallowing it here stops the crawl before it starts — belt and braces,
# and it keeps /private out of crawl logs and link reports.
Disallow: /private

# Its previous home. The redirects to /private are still in place, so the old
# prefix stays disallowed for as long as they are.
Disallow: /gestao

Sitemap: https://fsautomotive.pt/sitemap.xml
